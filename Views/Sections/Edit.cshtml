@model KlangIT_V3.ViewModels.SectionEditViewModel
@{ ViewData["Title"] = "แก้ไขหน่วยงาน"; }
@await Html.PartialAsync("~/Views/Shared/_DesignSystem.cshtml")

<h1 class="ds-page-title">แก้ไขหน่วยงาน</h1>
<div class="ds-card" style="max-width:400px">
    <form asp-action="Edit">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="alert alert-danger small mb-3"></div>
        <input type="hidden" asp-for="Id"/>
        <div class="ds-row">
            <label asp-for="SelectedDepartmentId" class="ds-lbl">ฝ่าย/กลุ่มงาน</label>
            <select asp-for="SelectedDepartmentId" class="ds-select" asp-items="Model.Departments"></select>
            <span asp-validation-for="SelectedDepartmentId" class="text-danger small"></span>
        </div>
        <div class="ds-row">
            <label asp-for="Name" class="ds-lbl">ชื่อหน่วยงาน</label>
            <input asp-for="Name" class="ds-input"/>
            <span asp-validation-for="Name" class="text-danger small"></span>
        </div>
        <div class="ds-actions">
            <button type="submit" class="ds-btn-primary">บันทึก</button>
            <a asp-action="Index" class="ds-btn-ghost">ยกเลิก</a>
        </div>
    </form>
</div>
@section Scripts { @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); } }
