@{
    ViewData["Title"] = "จัดการสถานะของอุปกรณ์/ครุภัณฑ์";
}

<div class="container py-4">

    <!-- Page Header -->
    <div class="d-flex align-items-center justify-content-between mb-4">
        <div>
            <h1 class="fs-3 fw-bold mb-1">
                <i class="bi bi-box-arrow-up-right text-primary me-2"></i>จัดการสถานะของวัสดุ/ครุภัณฑ์
            </h1>
        </div>
    </div>

    <div class="row g-4">

        <!-- Left: Reference Image Card -->
        <div class="col-lg-5">
            <div class="card shadow-sm border-0">
                <div class="card-body d-flex flex-column align-items-center justify-content-center">
                    <img src="~/Images/ItemStatesThai_V2.png"
                         class="img-fluid rounded"
                         data-bs-toggle="modal"
                         data-bs-target="#imgModal"
                         style="max-width:100%; max-height:320px; object-fit:contain; cursor:zoom-in;" />
                    <div class="text-center text-muted small mt-2">
                        <i class="bi bi-zoom-in me-1"></i>คลิกที่รูปเพื่อขยาย
                    </div>
                </div>
            </div>
        </div>

        <!-- Right: Borrow Form Card -->
        <div class="col-lg-7">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-white border-0 pt-3">
                    <h5 class="card-title mb-0 fw-semibold">
                        <i class="bi bi-pencil-square text-primary me-2"></i>รายละเอียดการจัดการสถานะของวัสดุ/ครุภัณฑ์
                    </h5>
                </div>
                <div class="card-body">
                    Content Here
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Image Zoom Modal -->
<div class="modal fade" id="imgModal" tabindex="-1" aria-labelledby="imgModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-xl">
        <div class="modal-content border-0 bg-transparent">
            <div class="modal-body text-center p-0">
                <img src="~/Images/ItemStatesThai_V2.png"
                     class="img-fluid"
                     data-bs-dismiss="modal"
                     style="cursor:zoom-out;" />
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}